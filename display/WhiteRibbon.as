package display{	import flash.display.*;	import flash.events.*;	import a24.tween.Tween24;	public class WhiteRibbon extends MovieClip	{		//Properties		//Member variable		protected var shape:Shape;		//Constructor		public function WhiteRibbon()		{			if (stage) init(null);			else addEventListener(Event.ADDED_TO_STAGE,init);		}		/**		* init		*/		private function init(ev:Event):void		{			removeEventListener(Event.ADDED_TO_STAGE,init);			//白帯のシェイプを作る			shape = new Shape;			shape.graphics.beginFill(0xFFFFFF);			shape.graphics.drawRect(0,0,1200,2160);			shape.graphics.endFill();			shape.x = shape.y = 0;			shape.height = 0;			shape.alpha = .7;			addChild(shape);		}		/**		* show		*/		public function show()		{			Tween24.tween(shape,.7,Tween24.ease.ExpoInOut).height(2160).play();		}		/**		* hide		*/		public function hide()		{			Tween24.tween(shape,.5,Tween24.ease.ExpoInOut).height(0).play();		}	}}